# Python dependencies for HOST-SIDE development tools
# These are NOT for the Pico - MicroPython code in src/ has no dependencies
#
# Install with: pip install -r requirements.txt
# Or with dev dependencies: pip install -r requirements.txt -r requirements-dev.txt

# Serial communication with Pico (for devops scripts)
pyserial>=3.5

# Audio file processing (convert/prepare MP3 files for DFPlayer)
pydub>=0.25.1

# Note: pydub requires ffmpeg or libav
# Install ffmpeg: sudo apt install ffmpeg (Linux) or brew install ffmpeg (Mac)
